import { Box } from "@mui/material"

export function DetailProductComp({ productId }: { productId: string }) {
    console.log('productId', productId)
    return <Box>
        {'Detail Product'}
    </Box>
}